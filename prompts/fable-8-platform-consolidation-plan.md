# Fable 8 — Platform consolidation: finalize plan, write next phase prompts

**Model:** Fable 5.1. **Approved for this phase because:** Anton opened this window himself and this is planning/spec work (finalizing architecture, judgment calls, phase sequencing) — exactly the kind §"approved Fable work" in `.claude/skills/fable-cost-guardrail/SKILL.md` (v2) allows. This is NOT a scaffolding/CRUD/content phase.

**Cost discipline — read this before doing anything else:** Everything you need is already gathered below. Do NOT re-explore `pararesi` or `flyttatillparaguay` on disk, do NOT re-derive the schema comparison, do NOT re-read plan.md end to end before starting — §1 (unchanged) and §12 (the draft) already contain everything a prior Sonnet research pass produced. Your job is judgment + writing, not discovery. Budget: one sitting. If you find yourself grepping through `pararesi/src` or `flyttatillparaguay/content`, stop — that means something below is missing and you should ask Anton rather than re-derive it.

**Your task, in order:**

1. Read `plan.md` §12 in this repo (the proposed consolidation section) — it is already written and merged into this branch. It covers: new SiteKeys for `paraguayfrontier.com`, `residenciaparaguay.es`, `residencianoparaguay.com`, `flyttatillparaguay.se`; locale becoming a real type; the full table-by-table schema merge proposal bringing `pararesi`'s member/tier/course-drip model into `src/db/schema.ts`; the payment-provider decision (keep Stripe + Lemon Squeezy both, unify later); the `flyttatillparaguay` migration plan.
2. Two decisions are already locked by Anton (2026-09-07, recorded in §1.5/§1.8/§8/§12.1): the Guide (`paraguayinvestorguide.com`) price is **$7**, and `pararesi`'s tripwire + Insider membership product sells **through the `guide` SiteKey** — no separate domain, no separate SiteKey for pararesi's content. `realestateinparaguay` is confirmed **out of scope** (different business, real estate not residency, stays its own app).
3. Answer the remaining open questions in §12.6 (items 2–6) yourself, using your judgment as the plan author — these are exactly the ambiguous-design-decision calls this phase exists for:
   - Q2: pick one platform-wide product/tier taxonomy (recommend something like `entry` ($7, ex-guide) / `insider` (recurring, ex-pararesi-Insider) / `service` (the high-ticket residency/investor-pass work, not tier-gated, lead-based) — but decide and commit, don't leave it open).
   - Q3: name the SiteKeys for `frontier`, the Spanish brand, and the Portuguese brand. Keep them short, lowercase, matching the existing style (`residency`, `investorpass`, `guide`).
   - Q4: decide positioning for the ES and PT brands — same hub content localized, or distinct enough copy/offer to need their own §11-style key-copy section. If distinct, write a compact key-copy block for each (H1, sub, 3 value points, meta title/description) in the same style as §11.1–§11.3 — this is exactly the "key positioning copy" work this skill approves for Fable.
   - Q5/Q6: decide the phase sequencing and whether this needs its own phase-table row(s). Recommend (but you decide): one Opus phase for schema/locale/registry foundation work (schema merge, `SiteKey` union expansion, i18n plumbing, entitlements lib) before any new Sonnet content phases, since schema/middleware/auth/payments are explicitly off-limits to Sonnet phases per plan §6/§4.7.
4. Write your decisions directly into `plan.md`: promote §12 out of "PROPOSED" — fold the now-decided pieces into §1 (locked decisions) and §2 (object model/schema), delete the resolved items from §12.6, update the phase table at the top of the file with the new phase(s) (model, prompt file name, plan sections, "how it starts").
5. Write the actual prompt file(s) for the new phase(s) you just added to the phase table — same format as the existing `prompts/opus-*.md` / `prompts/sonnet-*.md` files (self-contained: skills to load, exact tasks, exit criteria). If you decided on one Opus foundation phase, write `prompts/opus-3-consolidation-foundation.md` covering: expand `SiteKey` union + registry entries for the 4 new domains, expand `locale` type + i18n message trees, merge the schema per §12.3 into `src/db/schema.ts` + migration, build `src/lib/entitlements.ts` (tier gating, ported from pararesi's decay/grace logic), wire both Stripe and Lemon Squeezy into the generalized `purchases`/`subscriptions`/`webhookEvents` tables. Follow the existing Opus phase files' level of detail (see `prompts/opus-1-foundation.md`, `prompts/opus-2-conversion-core.md` for the format/tone to match).
6. Append a dated entry to plan.md §9 (build log) recording what this phase decided and wrote, same format as existing entries.
7. **Do not spawn this next phase.** Per the cost guardrail, Fable phases are never spawned and never spawn a following Fable phase. End with a short report to Anton: what you decided (the taxonomy, the SiteKey names, the sequencing), what you wrote (plan.md diff summary, new prompt file(s)), and the one line he pastes into a fresh Opus window to start the next phase — matching the handoff format in plan.md §4.9.

**Guardrails while you work:**
- Never spawn a subagent, session, workflow, or trigger on Fable — only Opus or Sonnet (guardrail core rule).
- Don't touch pricing, scope, or the pararesi/guide-merge decision — those are locked, not yours to revisit.
- Don't write page content or scaffolding yourself — that's what the Opus/Sonnet phase you're about to spawn is for. If you catch yourself writing component code, stop.
- Keep this phase small: judgment + the plan/schema-design doc + phase prompt file(s) + build log entry. If a decision genuinely needs Anton (not just "would be nice to confirm"), say so in the final report instead of guessing.
