**2026-09-15 — S22 Funnel fixes (Sonnet manager/Codex worker)** — PR: https://github.com/antonmarklundcom/paraguayresidency/pull/40

What now exists: `src/features/quiz/questions.ts`'s `ROUTE_DESTINATIONS` is keyed by `SiteKey` first, then `Route` (`src/features/quiz/scoring.ts` untouched throughout, as the plan requires). Every quiz result now prefers the visitor's own brand's page — frontier's combined `/routes` page (existing `#temporary`/`#permanent` anchors), residenciaes's and residenciapt's own service pages, flytta's single combined `/uppehallstillstand` page — instead of always routing to the hub. The Investor Pass outcome still always crosses to investorpass's own page for every brand, by design, unchanged. Guide's insider FAQ reads the live Guide price via the same pattern its homepage already used, instead of a hardcoded `$7`.

All five pricing pages (residency, residenciaes, residenciapt, flytta, frontier — all five previously bare "TODO" price tables) now explain, per route, what the fixed fee covers, what it never covers, how quoting actually works, and what's paid to the state versus the service — every figure a `<Fact k="pricing.*">`, one key set shared across brands, all `verified: false`. A new `ProcessTimeline` component (steps, who-does-what, durations sourced from existing or new hedged Facts) plus a "who files your case" trust block (reusing the exact framing already live on frontier's and investorpass's about pages, no invented claims) is wired onto 36 service-shaped pages across all seven brands — guide correctly excluded, it sells reference material rather than filing a case. O19's WhatsApp-only lead variant, built but never placed, now sits beside the full contact form on residenciaes/residenciapt/frontier/flytta's contact and home pages. No testimonials anywhere — confirmed by grep across every touched file, since that stays gated to a later phase. 601 tests, was 552.

Decisions worth reading:
- investorpass and guide's standard-residency quiz outcomes still route to the hub, not their own brand — neither has its own temporary/permanent-style service page (investorpass sells investment-based permanent residency instead, which is exactly the separate investor-pass route already).
- Three new timeline Fact keys were needed (`residency.timeline`, `tax.timeline`, `investorpass.timeline`) where no existing key covered a route; `cedula.timeline` and `temporary.duration` were reused everywhere they already fit rather than duplicated.
- The pricing rebuild's test (`tests/pricing.test.ts`) walks each page's real AST with the TypeScript compiler API to assert no digit or currency symbol appears outside markup - not a regex guess.

Pre-handoff audit (§4.9): `npm run verify` re-run green (601 tests); every commit independently re-verified by actually running typecheck/i18n/tests/build rather than trusting the worker's own report — its sandbox hit the same registry/memory restrictions on `verify:i18n` across all three dispatches this phase took and reported that honestly each time. Beyond the automated suite: independently grepped every touched file for testimonial/review/blockquote patterns (found none) and read actual rendered page source for a contact page and a service page to confirm the full contact form was retained (not replaced) and `ProcessTimeline` received the correct route per page — not just that tests passed.

Where S23 looks first: `content/shared/facts.ts` now has `pricing.*` and the three new `*.timeline` keys alongside the pre-existing ones — reuse rather than duplicate if content work needs a similar hedged figure. The `site.tagline` follow-up task from S21 (i18n key with no translation anywhere) is still unclaimed.
