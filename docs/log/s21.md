**2026-09-15 — S21 SEO surfaces (Sonnet manager/Codex worker)** — PR: https://github.com/antonmarklundcom/paraguayresidency/pull/39

What now exists: every brand has a crawlable article index — single-hub brands (investorpass/insights, frontier/stories, flytta/guider, flytta/stader) get one index page; multi-hub brands (residency/guides, residenciaes/guias, residenciapt/guias) get a top-level all-hubs index plus a per-hub index. Each lists real articles with title/date/description, `Breadcrumbs`, and a new `collectionPageJsonLd` builder in `src/lib/metadata.ts`. Every brand also has a working RSS 2.0 feed at `/feed.xml` (`src/lib/rss.ts`), with `<link rel=alternate>` discovery added to `siteMetadata`. `frontier`'s `routes`/`tax`/`why-paraguay` pages emit `Service` JSON-LD like the other brands. `residenciaes` and `residenciapt` now link their previously orphaned pages (`/mercosur`, `/pase-inversor`, `/precos`, `/investor-pass` — all pre-existing, just not discoverable in nav/footer) plus their new `/guias` indexes. `residenciaes` has WhatsApp click-to-chat on its home, contact and five service pages. Clean build: 154/183 `sites/**` routes static = 84.2%, up from O19's 81.5% — every new page here is a plain content listing with no request-time data. 530 tests, was 495.

Decision worth reading: found and fixed a real, pre-existing bug while building the RSS feeds, unrelated to this phase's own scope. `src/sites/registry.ts`'s `tagline` field is an **i18n key name** (every brand's value is the literal string `'site.tagline'`, consumed via `t(site, config.tagline)` in `Footer.tsx` and guide's homepage), not literal display text. The first draft of `rss.ts` used it as literal text; fixed to call `t()` like the rest of the codebase. The deeper gap this exposed — the i18n key `site.tagline` has no translation in **any** locale's `common.json`, so `Footer.tsx` and guide's homepage have been silently rendering blank (or the raw key name outside production) at that spot since whichever phase first wrote them — is real content work, not fixed here; flagged as a follow-up task rather than writing tagline copy for seven brands unprompted.

Pre-handoff audit (§4.9): `npm run verify` re-run green (530 tests); every commit independently re-verified by actually running typecheck/tests/build/i18n rather than trusting the worker's own report — its sandbox hit registry/memory restrictions on `verify:i18n` and `drizzle-kit`-adjacent checks across several dispatches this project and reported that honestly each time rather than fabricating results. The static percentage was recomputed independently after both parts of this phase using the same counting method O19 established, since the worker's own self-reported numbers were inconsistent between runs (84.2% vs. 81.6% for byte-identical build output) — always trust a fresh independent count over either side's arithmetic.

Where S22/S23 look first: `src/lib/rss.ts` and the `collectionPageJsonLd` builder in `src/lib/metadata.ts` if either phase adds more listable content types. The `site.tagline` follow-up task (see the spawned suggestion) is unclaimed — either phase could pick it up if it touches `Footer.tsx` or brand copy anyway, or it can wait for a dedicated content pass.
