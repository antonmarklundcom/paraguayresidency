**2026-09-15 — S23 Content depth (Sonnet manager/Codex worker)** — PR: https://github.com/antonmarklundcom/paraguayresidency/pull/41

What now exists: five ~100-word public stubs (`is-paraguay-residency-worth-it`, `what-the-investor-pass-is`, `sa-gar-flytten-till`, `rutas-de-residencia`, `rotas-de-residencia-temporaria-permanente-mercosul`) are now 1,030–1,110-word real comparison articles, each with two `related` links to genuinely relevant existing articles. All thirteen non-deep-dive `content/guide/members/` lessons went from 169–258 words to 813–949, matching the established chapter voice from `getting-started/why-paraguay-and-why-not.mdx` and its inline chapter-name cross-reference convention (not the public blog's frontmatter `related` field). The three Insider `updates/` pages and the deep dive went from 86–229 words to 1,127–1,196, each 1000–1500 words with either a genuine anonymised worked case (British and German applicant scenarios, deep dive and document-requirement-note) or an honestly-reported alternative depth path where a case didn't fit naturally (insider-launch expanded on concrete membership value, whats-next on specific planned-article detail) — one new Fact key, `documents.police_certificate_validity` (`verified: false`), came out of that work. Six new nationality pillar articles now exist: four under `content/residency/documents/` (British, American, German, South African applicants) and two under `content/frontier/stories/` (US, Canada), each covering that nationality's apostille/legalisation chain shape, police-certificate issuer, and the existing local-sworn-translator rule, hedged throughout and reusing `documents.police_certificate_validity` and `residency.timeline` rather than inventing new figures — the two frontier pieces are deliberately angled around the document chain specifically, distinct from the pre-existing narrative case-study story (`american-first-90-days.mdx`) already in that hub. 601 tests, unchanged from S22 (content-only phase, no new test files).

Decisions worth reading:
- Two of the four Insider "top of funnel" pages (insider-launch, whats-next) do not contain a fabricated worked case — per instruction, honesty about the format not fitting naturally was preferred over forcing an artificial case just to check a box.
- The nationality pillar articles hedge the actual issuing-authority names rather than asserting them as settled fact, except where a generic, safely-stated fact already appears elsewhere in the site (e.g. Hague Apostille Convention membership) — each article does link out to that country's real official government authentication-guidance page as a citation, which is new: no prior content phase in this repo had linked to an external government source.
- No new Fact keys were needed for the six pillar articles beyond the one added in the deep-dive/updates work; every duration or figure reused `documents.police_certificate_validity` or `residency.timeline`.

Pre-handoff audit (§4.9): every one of the four dispatches this phase re-verified independently rather than trusting the worker's self-report — including a case where a dispatch's own exit code read "failed" (a mid-run network/DNS outage to the Codex backend) after all thirteen files had already been written and audited by the worker itself; verified via raw log inspection, `git diff`, word counts and a full local test/build run before trusting and committing it. A `tests/abuse-surfaces.test.ts` failure surfaced once during a full-suite run on content-only changes; re-running that single test file in isolation passed 13/13, confirming it was a flake unrelated to this phase's `.mdx`-only diff, not a regression. Part 4's own worker-reported test run showed one hygiene-test failure and an i18n ENOMEM error; both were the worker's own sandbox not having staged its new files (`git add` had failed with an index.lock permission error) and a known Windows-only transient memory error — both passed clean on independent re-run once the six new files were staged. `npm run verify` (build, full suite, i18n) re-run green after every one of the four parts.

Where S6/S15 look first: this phase closes out the F10 content-depth work per §7 — no public article under 600 words, no member lesson under 700, six new pillar articles exist. The `site.tagline` follow-up task flagged by S21 (i18n key with no translation anywhere) is still unclaimed.
