**2026-09-15 — S20 Quality gates & repo hygiene (Sonnet manager/Codex worker)** — PR: https://github.com/antonmarklundcom/paraguayresidency/pull/33

What now exists: `tests/content.test.ts` enforces title ≤ 60, description ≤ 155 (was 70/160), using `expect.soft` so every offending file shows in one run; 15 files trimmed to fit, title/description only. `tests/fact-site.test.ts` catches the S11 bug class (a `<Fact>` under a non-`en` locale group missing an explicit `site=` prop, which silently falls back to English) via a source-text scan robust to multiline props, expressions and comments. `tests/repo-hygiene.test.ts` fails a build that leaves an unexpected untracked file behind, with a pattern-based (not hand-maintained) allowlist for `docs/log/*.md`. `package.json` `engines: {"node": ">=22"}`, `.nvmrc` = 22, `@types/node` `^20` → `^22`. 21 new pure unit tests for `metadata.ts`, `whatsapp.ts`, thank-you download tokens, subscriber confirmation/unsubscribe tokens. CI gained four things: `npm audit --omit=dev --audit-level=high`, `.github/dependabot.yml` (weekly, grouped minor/patch), a migration-drift job (`drizzle-kit generate` must produce no new file), and a PR-only Lighthouse job running O19's `tests/lighthouse.mjs` (`continue-on-error: true` for its first two weeks, with a reconsideration-date comment). `plan.md`'s §9 (~760 of 1624 lines) is now a one-line-per-phase index; every dated entry moved verbatim into `docs/log/<id>.md` (18 files at the time, word-for-word verified against git history for several). `KNOWN-ISSUES.md`'s 8 CLEARED/FIXED entries moved into `docs/known-issues-archive.md`. 512 tests, was 469.

Decisions and deviations:
- `plan.md`'s line count did not reach the phase's own <700 target (ended at 885, including a retroactive O19 entry — see below): reaching 700 would mean cutting real decisions or requirements from sections outside this phase's authorized scope. Reported honestly rather than trimming content to hit a number.
- O19 never got its own §9 entry when it merged — a gap from that phase, caught and fixed while doing this one's docs split (`docs/log/o19.md` added retroactively, same format as the rest).
- Hit the same npm cross-platform lockfile bug O19 already fixed once (`vite`'s `esbuild` peer want getting incorrectly deduped, `npm ci` failing on Linux with `EBADPLATFORM` even though it passes locally on Windows) — confirmed it recurs any time a plain `npm install` runs, not a one-time fix. Regenerated from scratch and re-patched twice during this phase; each time confirmed against the real Linux CI run, not just local success.

Pre-handoff audit (§4.9): `npm run verify` re-run green (512 tests before S21 started); every commit independently re-verified by actually running the commands, not trusting the worker's report — its sandbox hit real npm registry and memory restrictions on several checks (`npm audit`, `drizzle-kit generate`, `verify:i18n`) across multiple dispatches and reported that honestly each time.

Where S21/S22/S23 look first: after any `npm install` in this repo, run `npm ls esbuild` and check for an "invalid" dedup before trusting the lockfile — this is a real, recurring npm bug in this dependency tree, not paranoia. `tests/repo-hygiene.test.ts`'s allowlist needs a new pattern or entry for any new top-level file/directory a phase adds outside `docs/log/`. `docs/log/` and `docs/known-issues-archive.md` are now where phase history and cleared issues actually live — `plan.md` §9 and the root `KNOWN-ISSUES.md` are indexes, not full logs.
